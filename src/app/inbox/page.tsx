import Link from "next/link";
import { currentUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { PhoneFirstEntry } from "@/components/PhoneFirstEntry";
export default async function Inbox(){const user=await currentUser();if(!user)return <PhoneFirstEntry/>;const result=await db().from("conversations").select("id,buyer_id,seller_id,listing:listings(title,image_urls),buyer:users!buyer_id(name),seller:users!seller_id(name)").or(`buyer_id.eq.${user.id},seller_id.eq.${user.id}`).order("created_at",{ascending:false});const conversations=result.data||[];return <div className="panel"><h1>Inbox</h1>{conversations.length===0?<p className="muted">No conversations yet. Ask Dibs to find something and message a seller.</p>:conversations.map((c:any)=><Link className="inbox-item" href={`/conversations/${c.id}`} key={c.id}><strong>{c.listing.title}</strong><div className="meta">With {c.buyer_id===user.id?c.seller.name:c.buyer.name}</div></Link>)}</div>}